sudo: false

language: scala

jdk:
  - openjdk8
  - openjdk11

scala:
  - 2.12.8

cache:
  directories:
    - $HOME/.m2/repository
    - $HOME/.ivy2/cache
    - $HOME/.coursier/cache
    - $HOME/.sbt

env:
  - LOG_LEVEL=INFO

script:
  - sbt ++$TRAVIS_SCALA_VERSION clean test
