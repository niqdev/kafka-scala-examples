sudo: false

cache:
  directories:
  - $HOME/.m2/repository
  - $HOME/.sbt
  - $HOME/.ivy2

language: scala

jdk:
  - openjdk8

scala:
  - 2.12.7

env:
  - LOG_LEVEL=INFO

script:
  - sbt ++$TRAVIS_SCALA_VERSION clean test
